interface:
  display_name: "Create Context"
  short_description: "Create a context from local knowledge"
  default_prompt: "Use $neatcontext:create to create a context from an existing knowledge folder."

policy:
  allow_implicit_invocation: false
