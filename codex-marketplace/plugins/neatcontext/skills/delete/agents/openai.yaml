interface:
  display_name: "Delete Context"
  short_description: "Delete a local context safely"
  default_prompt: "Use $neatcontext:delete to delete a context after preview and confirmation."

policy:
  allow_implicit_invocation: false
