interface:
  display_name: "Import Context"
  short_description: "Import a shared lite context bundle"
  default_prompt: "Use $neatcontext:import to import a shared lite context bundle."

policy:
  allow_implicit_invocation: false
