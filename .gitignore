node_modules/
*.log
.DS_Store
.neatcontext-capture-*.json

# Written by Claude Code when a plugin path stops being the recorded install
# target. Harmless, but this working tree doubles as a local-dev install
# source, so the marker lands here instead of in the plugin cache.
.orphaned_at
